{% extends "basepro.html.twig" %}

{% block body %}
	<div class="container mt-3">
		<h1>{{service.name}}</h1>
		<div class="row">
			<div class="col">


				<img src="{{ asset('uploads/' ~ service.image) }}" width="auto" height="auto" class="rounded d-block w-50" alt="image service">
			</div>
			<div class="col">
				<p>
					{{service.des}}</p>
			</div>
		</div>
		<div class="d-flex justify-content-center">
			<a href="{{ path('app_quote') }}" class="btn btn-success">Faire un devis</a>
		</div>
	</div>
{% endblock %}
