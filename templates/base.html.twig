<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}EcoService{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
        <link href="{{ asset('bootstrap/css/bootstrap.min.css') }}" type="text/css" rel="stylesheet">
        <link href="{{ asset('css/styles.css') }}" type="text/css" rel="stylesheet">
        {% endblock %}

    </head>
    <header>
        {{ include('assets/headerparticular.html.twig') }}
        <nav class="nav nav-pills flex-column flex-md-row bg-dark">
            <a class="flex-sm-fill text-sm-center nav-link link-light" href="#">FAQ</a>
            <a class="flex-sm-fill text-sm-center nav-link link-light" href="#">Contact</a>
        </nav>
    </header>
    <body>
        {% block body %}

        {% endblock %}
    </body>
   {{ include('assets/footer.html.twig') }}
    {% block javascripts %}
            <script src="{{ asset('bootstrap/js/bootstrap.min.js') }}"></script>
    {% endblock %}
</html>
